#include<stdio.h>
int main(){
    // int a, b, sum;
    // printf("Enter the value of integers\n");
    // scanf("%d %d", &a, &b);
    // sum = a + b;
    // printf("Sum = %d", sum);  
    
    //  int a, b, sub;
    // printf("Enter the value of integers\n");
    // scanf("%d %d", &a, &b);
    // sub = a - b;
    // printf("Sub = %d", sub);  

    //  int a, b, mul;
    // printf("Enter the value of integers\n");
    // scanf("%d %d", &a, &b);
    // mul = a * b;
    // printf("Mul = %d", mul);  
    return 0;
}
